<!--
    Config.d override mounted only on the dedicated FIPS-compatible
    ClickHouse server container brought up by
    ``connectivity_against_fips_clickhouse_server``.

    Adds the secure native TCP listener on port 9440 with a
    FIPS-approved cipher set, matching the Altinity FIPS docs
    (https://docs.altinity.com/altinitystablebuilds/fips-compatible-altinity-builds/).
    The plain ``tcp_port`` (9000) and ``http_port`` (8123) are intentionally
    left as image defaults so the container's healthcheck (``/ping`` on
    8123) keeps working without depending on TLS.

    Server certificate / key paths point at ``/etc/clickhouse-server/ssl/``
    which the test bind-mounts from ``configs/clickhouse/ssl/``
-->
<clickhouse>
    <tcp_port_secure>9440</tcp_port_secure>
    <openSSL>
        <server>
            <certificateFile>/etc/clickhouse-server/ssl/server.crt</certificateFile>
            <privateKeyFile>/etc/clickhouse-server/ssl/server.key</privateKeyFile>
            <cipherList>ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:AES128-GCM-SHA256:AES256-GCM-SHA384</cipherList>
            <cipherSuites>TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384</cipherSuites>
            <preferServerCiphers>true</preferServerCiphers>
            <disableProtocols>sslv2,sslv3,tlsv1,tlsv1_1</disableProtocols>
            <verificationMode>relaxed</verificationMode>
        </server>
    </openSSL>
</clickhouse>
